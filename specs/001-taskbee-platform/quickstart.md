# Quickstart

- Run 
pm run dev and navigate to /marketplace to ensure the layout functions and appears properly.
- All styles should render relying on standard Tailwind or the legacy --sg-color palette injected into globals.css.
